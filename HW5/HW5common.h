/* Any definitions that apply to both client and server should be
 * in a common .h file
 */

#define MAX_LINE 1024 //The max number of characters each line
#define GOODCODE "good" //The response header for a successful exit
#define BADCODE "bad" //The response header for an unsuccessful exit